/**
 * everytime local my-component files change, you should re-run npm pack
 * to generate the latest .tgz package
 * 每次当本地的my-component组件文件发生变化时，你应当重新运行npm pack以生成最新的.tgz包
 */
// if use import syntax（如果使用的是import语法）
import 'my-component'

// or if use require（如果使用的是require）——AMD/Commonjs
// require('my-component')
